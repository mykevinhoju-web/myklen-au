# Brand wordmark (required for production)

Header text logo: **`myklen-wordmark.png`**

If the live site still shows plain text or a broken image, this folder was not pushed to GitHub. Run:

```bash
git add public/brand
git commit -m "Add myklen wordmark logo"
git push
```

Then redeploy on Vercel.
